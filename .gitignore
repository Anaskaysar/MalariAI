# ============================================================
# MalariAI — .gitignore
# Public repo: code + results JSONs only.
# Data, checkpoints, internal docs, and build artifacts stay local.
# ============================================================

# ── Virtual environments ─────────────────────────────────────
malariaenv/
venv/
.venv/
env/

# ── Datasets (download separately per dataset licence) ───────
data/malaria/images/
data/malaria.zip
data/*.zip
data/crops/
data/MP-IDB/img/
data/MP-IDB/meta/
# Annotation JSONs and CSVs are small and kept:
# data/MP-IDB/ann/  ← kept (JSON annotations)
# data/processed/   ← kept (derived CSVs, reproducibility)

# ── Model checkpoints (too large; host on HuggingFace Hub) ───
*.pth
*.pt
*.ckpt

# ── Phase-specific artefacts ──────────────────────────────────
Phase2-BaselineA/checkpoints/
Phase2-BaselineA/checkpoints-kaggle-80epoch/
Phase2-BaselineA/checkpoints-local-20epoch/
Phase2-BaselineA/results/
Phase2-BaselineA/0.8
Phase3-PipelineB/checkpoints/
Phase3-PipelineB/checkpoints_v2/
Phase3-PipelineB/Phase3-PipelineB/
Phase4-WebApp/checkpoints/
Phase4-WebApp/static/uploads/
Phase5-YOLO-Baseline/**/checkpoints-kaggle-80epoch/weights/
**/yolo_data/images/

# ── Internal working documents (not for public) ───────────────
paper_writing/
1.Revisions/
31st May/
CMIG_Final/
malariai_medical_ui.html
malariai_ui_proposal.html
email_to_professor.md
author_guide.pdf
references.bib

# ── Nested deployment repo (separate HF Space git repo, not a submodule) ──
MalariAI/

# ── LaTeX build artefacts (compile on Overleaf) ───────────────
*.aux
*.log
*.out
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.toc
*.pdf

# ── Utility / scratch scripts ─────────────────────────────────
scripts/
archive/
problem_statement.md
run.bat
run.ps1
run.sh

# ── Temporary eval runs ───────────────────────────────────────
results/v2/e2e_bbbc041_altmetrics/

# ── Python cache ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
.Python

# ── Ultralytics/YOLO regenerable cache ──────────────────────────
*.cache

# ── Jupyter ───────────────────────────────────────────────────
.ipynb_checkpoints/

# ── Packaging ─────────────────────────────────────────────────
dist/
build/
*.egg-info/
*.egg
wheels/

# ── Testing ───────────────────────────────────────────────────
.pytest_cache/
.coverage
htmlcov/

# ── IDEs ──────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── OS ────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
Thumbs.db
ehthumbs.db
desktop.ini

# ── Logs / wandb ──────────────────────────────────────────────
*.tmp
*.temp
wandb/
runs/
lightning_logs/
